# Dependencias para entrenamiento cloud (Kaggle Notebooks ya tiene la mayoría)
# Instalar con: pip install -r requirements.txt
# En Kaggle solo se necesita: !pip install optuna --quiet

xgboost>=2.0.0
optuna>=3.0.0
pandas>=2.0.0
pyarrow>=12.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
matplotlib>=3.7.0
scipy>=1.10.0
